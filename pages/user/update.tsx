import { MainLayout } from '../../src/components';
import { ProfileUpdateScreen } from '../../src/screens';

const User: React.FC = () => (
    <MainLayout>
      <ProfileUpdateScreen/>
    </MainLayout>
);

export default User;
